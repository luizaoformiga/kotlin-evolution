// Funçoes

// aqui mostraremos aluns exemplos de funçoes

private fun getFullNameOne(name: String, lastName: String): String {
    val fullName = "$name $lastname"
    return fullname
}

private fun getFullNameTwo(name: String, lastName: String): String {
    return "$name $lastName"
}

private fun getFullNameThree(name: String, lastName: String): String = "$name $lastName" 
