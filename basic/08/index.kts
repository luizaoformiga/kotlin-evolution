// metodos empty e blank

val some =
        println(some.isEmpty()) // true

println(some.isBlank()) // true

println(some.isNullOrBlank() || isNullOrEmpty) // true

// metodos de comparaçao
// string esta vazia, em branco ou e nula?

